{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AI Prompt Schema",
  "description": "Schema for AI prompt YAML files",
  "type": "object",
  "required": ["title", "prompt", "creativity", "icon", "model"],
  "properties": {
    "title": {
      "type": "string",
      "description": "The title of the prompt",
      "minLength": 1,
      "maxLength": 100
    },
    "prompt": {
      "type": "string",
      "description": "The prompt text with optional placeholders",
      "minLength": 1
    },
    "creativity": {
      "type": "string",
      "description": "The creativity level for the AI response",
      "enum": ["low", "medium", "high"]
    },
    "icon": {
      "type": "string",
      "description": "The icon identifier for the prompt",
      "minLength": 1
    },
    "model": {
      "type": "string",
      "description": "The AI model to use",
      "minLength": 1
    },
    "highlightEdits": {
      "type": "boolean",
      "description": "Whether to highlight edits in the response"
    }
  },
  "additionalProperties": false
}
