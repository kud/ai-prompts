{
  "name": "ai-prompts",
  "version": "1.5.1",
  "description": "Your one-stop collection of frequently used prompts specially curated for AI interactions.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "prebuild": "trash dist/",
    "build": "node src/index.js",
    "dev": "nodemon --ext yml --exec npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "js-yaml": "4.1.0"
  },
  "devDependencies": {
    "nodemon": "3.0.1"
  }
}
